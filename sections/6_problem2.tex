\section{问题二的模型建立与求解}

\subsection{问题分析}

问题二要求在经典标度律 $L(N,D)$ 基础上引入数据质量 $Q$，建立广义标度律
$L(N,D,Q)$，并利用 B 族数据估计参数、验证跨家族泛化、分析弹性与替代关系。
数据侧：B1 为 Pythia 家族 1176 条训练轨迹（拟合经典形式），B2 为 Cerebras
训练日志（半合成，做族外验证），B4/B5 为缩放基准与文献数据（跨家族/文献
验证），B6/B7 为 $N$--$D$--$Q$ 半合成实验（拟合广义形式），B8 为大规模
外推补充（结构对照），B10 为大模型基线（极端规模外推验证）。

\subsection{经典标度律}

\subsubsection{模型}

经典标度律（Kaplan--Hoffmann 形式）：
\begin{equation}
L(N,D)=E+A N^{-\alpha}+B D^{-\beta},
\label{eq:classical}
\end{equation}
其中 $E$ 为不可约误差，$A N^{-\alpha}$、$B D^{-\beta}$ 分别刻画参数量与
数据量的不足。对 B1 的 1176 条轨迹用带边界的最小二乘（scipy
\texttt{least\_squares}）拟合，结果：
\begin{equation}
E=1.690,\quad A=0.354,\quad \alpha=0.340,\quad B=1.240,\quad \beta=0.280,
\qquad R^2=0.999.
\label{eq:classical_fit}
\end{equation}
$\alpha=0.34$、$\beta=0.28$ 与 Chinchilla 论文报告值高度一致，说明 B1 数据
忠实复现了真实训练缩放规律，拟合质量极高。

\begin{figure}[htbp]
\centering
\includegraphics[width=0.85\textwidth]{figures/p2_classical_fit.png}
\caption{经典标度律在 B1 轨迹上的拟合与跨家族验证。}
\label{fig:p2_classical}
\end{figure}

\subsubsection{跨家族与文献验证}

不同家族（Pythia/Cerebras/其他）的验证集与词表不同，绝对 Loss 存在不可
比的常数偏移。为此定义\textbf{家族偏移修正 $R^2$}：
\begin{equation}
R^2_{\text{off}}=1-\frac{\sum_i\bigl(y_i-\hat y_i-\mu\bigr)^2}{\sum_i
(y_i-\bar y)^2},\qquad \mu=\frac{1}{n}\sum_i(y_i-\hat y_i),
\label{eq:r2off}
\end{equation}
即先消除整体偏差再衡量形状拟合。验证结果：

\begin{table}[htbp]
\centering
\caption{经典标度律跨数据验证}
\begin{tabular}{c|cc}
\toprule
数据 & 原始 $R^2$ & 偏移修正 $R^2$ \\
\midrule
B1 Pythia（拟合） & 0.999 & --- \\
B2 Cerebras & $-5.07$ & 0.385 \\
B4 缩放基准 & 0.605 & 0.830 \\
B5 文献数据 & 0.731 & 0.733 \\
\bottomrule
\end{tabular}
\end{table}

B4/B5 的偏移修正 $R^2$ 达 0.73--0.83，说明标度律形状跨家族成立；Cerebras
(半合成) 的 0.385 较低，源于其合成数据与真实家族的系统性差异，属预期现象，
但形状方向仍正确。

进一步做\textbf{留一族交叉验证}：对 B4 中 12 个模型家族（57 个规模点），
每次以其余 11 族拟合经典标度律、留出族验证（偏移修正 $R^2$），平均
$R^2=0.888$（范围 0.338--0.997）。除 GPT2（样本点稀疏，仅 4 个）外，其余
11 族均在 0.79 以上；$\alpha,\beta$ 估计随留出族波动（$\alpha\in
[0.07,0.17]$），源于各家族 $N$--$D$ 协同变化导致的标识别（identifiability）
问题，但函数形式跨族稳健。该交叉验证排除了``标度律形状仅由单一家族支撑''
的疑虑。

\subsection{广义标度律}

\subsubsection{质量项的两种形式}

为使质量维度可退化（$Q=1$ 时回到经典形式），考察两种扩展：
\begin{align}
\text{加性形式:}\quad & L=E+A N^{-a}+B D^{-b}+C(1-Q)^{g},
\label{eq:add}\\
\text{交互形式:}\quad & L=E+A N^{-a}+B D^{-b}+C(1-Q)^{g}D^{-d},
\label{eq:int}
\end{align}
交互形式允许质量损失项随数据量 $D$ 衰减（$d>0$ 时），物理含义为：数据量
越充裕，质量不足的相对代价越低。

\subsubsection{拟合结果}

在 B6+B7（810 条 $N$--$D$--$Q$ 组合，$N\in[0.07,12]$B，
$D\in[10,600]$B，$Q\in[0.1,1.0]$）上拟合：

\begin{table}[htbp]
\centering
\caption{广义标度律候选形式对比（B6+B7）}
\begin{tabular}{c|cccccccccc}
\toprule
形式 & $E$ & $A$ & $a$ & $B$ & $b$ & $C$ & $g$ & $d$ & $h$ & $R^2$ \\
\midrule
加性 & 1.507 & 0.534 & 0.281 & 1.323 & 0.292 & 0.362 & 0.991 & & & 0.9716 \\
交互($D$) & 1.551 & 0.534 & 0.281 & 1.251 & 0.310 & 0.445 & 0.991 & 0.045 & & 0.9720 \\
\textbf{交互($N$)} & \textbf{1.640} & \textbf{0.404} & \textbf{0.305} & \textbf{1.323} & \textbf{0.292} & \textbf{0.356} & \textbf{0.991} & & \textbf{0.163} & \textbf{0.9791} \\
乘性 & 1.602 & 0.470 & 0.270 & 1.096 & 0.260 & 0.419 & 0.988 & & & 0.9784 \\
\bottomrule
\end{tabular}
\end{table}

对四种候选形式（加性、质量$\times D^{-d}$、质量$\times N^{-h}$、乘性因子）
统一拟合后，\textbf{质量--参数量交互形式}拟合优度最高
（$R^2=0.979$，比原加性形式高 0.008）：质量缺口惩罚项
$C(1-Q)^{g}N^{-h}$ 随参数量幂律衰减（$h=0.163$），其物理含义是——
\textbf{模型规模越大，对数据质量缺口的敏感性越低}（大模型能从海量参数中
部分弥补低质数据的缺陷）。$Q=1$ 时该项自动消失，退化为经典标度律，满足
可退化性要求。乘性形式 $R^2=0.978$ 与交互($N$) 接近，但不可退化至经典
形式，故最终选交互($N$) 为广义标度律：

\begin{equation}
L = E + A N^{-a} + B D^{-b} + C(1-Q)^{g} N^{-h}.
\label{eq:gen_choice}
\end{equation}

\textbf{留出交叉验证（模型选择的严格检验）}：为避免``形式越复杂越拟合''
的过拟合风险，对四种形式做 5 折 $\times$ 3 重复交叉验证，报告留出折上的
预测 RMSE（表 \ref{tab:p2_cv}）。加入质量项后留出 RMSE 下降
$53\%$（0.126$\to$0.060），交互($N$) 形式留出误差最低
（0.051），且优于乘性（0.052）与加性（0.060）——所选形式在留出数据上
同样最优，非单纯样本内拟合。

\begin{table}[htbp]
\centering
\caption{标度律形式的留出交叉验证（B6+B7, 5 折 $\times$ 3 重复）}
\label{tab:p2_cv}
\begin{tabular}{c|cc|cc}
\toprule
形式 & 留出 RMSE & $\pm$ std & 留出 $R^2$ & $\pm$ std \\
\midrule
经典（无质量项） & 0.1262 & 0.003 & 0.864 & 0.006 \\
加性（质量项） & 0.0596 & 0.001 & 0.970 & 0.001 \\
\textbf{交互($N$)} & \textbf{0.0511} & \textbf{0.001} & \textbf{0.978} & \textbf{0.001} \\
乘性 & 0.0519 & 0.001 & 0.977 & 0.001 \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[htbp]
\centering
\includegraphics[width=0.78\textwidth]{figures/v26_p2_cv.png}
\caption{标度律形式 5 折交叉验证的留出 RMSE 对比。}
\label{fig:p2_cv}
\end{figure}

\textbf{信息准则选择}（图 \ref{fig:p2_bic}）：为避免复杂度偏好，补充
BIC/AIC 形式选择（$n=810$）。interaction\_N 的 BIC 最优（$-4813.73$）；
次优 multiplicative 的 $\Delta\text{BIC}=20.36$（>10，决定性证据），其余
形式 $\Delta\text{BIC}=235$--244 被\textbf{决定性拒绝}。信息准则与留出
交叉验证结论一致：``质量--参数量交互''形式并非样本内过拟合，而是在惩罚
复杂度后仍然胜出的模型。

\begin{figure}[htbp]
\centering
\includegraphics[width=0.78\textwidth]{figures/v50_p2_bic_table.png}
\caption{六种形式的 BIC/AIC 选择：interaction\_N 决定性胜出。}
\label{fig:p2_bic}
\end{figure}

\begin{figure}[htbp]
\centering
\includegraphics[width=0.85\textwidth]{figures/p2_generalized_fit.png}
\caption{广义标度律在 B6+B7 数据上的拟合（质量--参数量交互形式，$R^2=0.979$）。}
\label{fig:p2_generalized}
\end{figure}

\textbf{参数不确定性（Bootstrap）}：对交互($N$) 形式做 case bootstrap
（300 次重抽，$n=810$），参数与弹性均给出 2.5--97.5 分位区间
（表 \ref{tab:p2_boot}，图 \ref{fig:p2_boot}）。质量缺口的规模衰减指数
$h=0.163\ [0.142,0.187]$ 与质量指数 $g=0.992\ [0.921,1.067]$ 均显著
异于 0/1 退化边界；弹性区间表明质量弹性显著高于规模弹性：
$|\varepsilon_Q|=0.146\ [0.142,0.151]$ vs $|\varepsilon_N|=0.060\
[0.059,0.061]$（区间不交叠）。严格等价的参数节省（保持损失不变）为
$0.216\ [0.210,0.224]$B（一阶线性近似 0.289B 因忽略 $N$ 的非线性而略高，
正文统一采用一阶口径以与弹性定义一致）。

\begin{table}[htbp]
\centering
\caption{广义标度律参数 Bootstrap 区间（$B=300$，2.5/97.5 分位）}
\label{tab:p2_boot}
\begin{tabular}{c|ccc}
\toprule
参数 & 中位数 & 下限 & 上限 \\
\midrule
$h$（质量$\times N^{-h}$） & 0.163 & 0.142 & 0.187 \\
$g$ & 0.992 & 0.921 & 1.067 \\
$a$ & 0.307 & 0.281 & 0.324 \\
$b$ & 0.293 & 0.247 & 0.328 \\
$\varepsilon_N$ & $-0.060$ & $-0.061$ & $-0.059$ \\
$\varepsilon_D$ & $-0.030$ & $-0.032$ & $-0.028$ \\
$\varepsilon_Q$ & $-0.146$ & $-0.151$ & $-0.142$ \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[htbp]
\centering
\includegraphics[width=0.8\textwidth]{figures/v33_p2_boot_ci.png}
\caption{广义标度律参数与弹性的 Bootstrap 置信区间。}
\label{fig:p2_boot}
\end{figure}

\subsubsection{B8 数据方向诊断与结构对照}

B8 为大规模外推补充数据（含 calibrated 984 条与 extrapolated 720 条）。
诊断发现 B8 的 $Q$ 列与 B6/B7 方向相反：同一 $N,D$ 组内 $Q$ 与 Loss 的
相关在 B6 中为 $-0.925$（质量越高损失越低），在 B8 中为 $+0.984$（质量
越高损失越高）。这说明 B8 的半合成生成器采用了与 B6/B7 相反的``质量缺失
度''口径，或 Loss 尺度不同，不能与 B6/B7 直接混用。因此将 B8 仅作结构
对照：单独拟合（取 $Q'\to 1-Q$ 映射后）加性/交互形式分别得到 $R^2=0.975$
与 $0.984$，函数形式同样成立，验证了广义标度律的函数形态在更大规模数据上
依然适用，但绝对参数不可混用。这一数据口径核验体现了对半合成数据局限性的
严谨处理。

\textbf{参数级复核}（图 \ref{fig:p2_b8refit}）：直接在 B8 原样（不映射）上
拟合 $interaction\_N$，参数\textbf{退化到边界}：$E=0.000$、$C=0.000$
（质量项被拟合器整体剔除）、$g=9.995$（撞上界），$a$、$b$ 由
0.306/0.292 骤降至 0.140/0.124；且六种形式在 B8 原样上的 $R^2$ \textbf{完全相同}
（0.274，均不含质量项）——说明 B8 原样的 $Q$ 列对``质量越低损失越高''
规律\textbf{不携带任何信息}，与组内相关 $+0.984$ 的原始诊断互证。这构成
``B8 不可直接混用''的形式化统计依据：直接拟合只会产生无质量的退化解。

\begin{figure}[htbp]
\centering
\includegraphics[width=0.8\textwidth]{figures/v40_p2_b8_refit.png}
\caption{B8 原样 vs B6+B7 的参数对比：B8 质量项退化（$E=C=0$，$g$ 撞界）。}
\label{fig:p2_b8refit}
\end{figure}

\subsubsection{极端规模外推}

在 B10（100B--10000B 大规模基线）上检验经典标度律外推，原始与偏移修正
$R^2$ 均为 1.000，说明标度律在大规模区间保持形状，可放心用于问题三在
$10^{19}$--$10^{24}$ FLOPs 预算下的优化外推。

\subsubsection{参数可辨识性（剖面似然）}

对选定的交互($N$) 形式做剖面似然检验：固定其余参数于最优值、逐点扫描单个
参数并记录剖面 $R^2$，由似然比准则（$\chi^2_{0.90,1}$ 阈值）给出 90\% 剖面
区间。结果表明：\textbf{质量交互指数 $h$ 被数据良好识别}（90\% 区间
$[0.160,0.170]$，宽度仅 0.01），缺口幂 $g$ 为 $[0.968,0.995]$；而参数量
指数 $a$、数据量指数 $b$ 的剖面似然较平坦（无阈值交叉），这与 B6+B7 中
$N$--$D$ 协同变化导致的经典标识别问题一致（留一族交叉验证同样观察到
$\alpha$ 跨族波动）。该检验说明：本文新增的质量维度参数（$h,g$）估计可信，
而经典规模指数的不确定性是标度律文献的固有现象，不影响结论方向。

\begin{figure}[htbp]
\centering
\includegraphics[width=0.88\textwidth]{figures/v14_p2_profile.png}
\caption{关键参数的剖面似然曲线（红色虚线为 90\% 似然比阈值）。}
\label{fig:p2_profile}
\end{figure}

\subsubsection{质量回报与规模交互（数值核验）}

固定数据量 $D=300$B 扫描质量 $Q\in[0.4,1]$、参数量 $N\in\{0.1,\ldots,10^3\}$B，
得到质量投资回报曲线（图 \ref{fig:p2_qreturns}）。三个核验结论：
（i）质量全幅收益（$Q:0.4\to1.0$ 的 $L$ 降幅）随 $N$ 从 0.31 衰减至 0.07，
即模型越大、质量缺口对损失的边际影响越小——正是交互($N$) 形式
$N^{-h}$ 项的语义；（ii）数值微分得到的质量弹性 $|dL/dQ|$ 对 $N$ 的
对数--对数斜率恰为 $-\hat h=-0.163$，与拟合指数 $h=0.163$ 完全吻合，
构成对参数 $h$ 的独立数值验证；（iii）质量从 0.4 提升到 0.6 的收益
等价于模型可缩小倍数（参数节省）随 $N$ 从 1.42 倍升至 2.48 倍——在
参数回报更弱的大规模区，同等质量改进的相对杠杆更大。

\begin{figure}[htbp]
\centering
\includegraphics[width=0.72\textwidth]{figures/v20_p2_qreturns.png}
\caption{质量投资回报曲线（$D=300$B）：$L(Q)$ 相对 $L(Q=1)$ 随 $N$ 变化。}
\label{fig:p2_qreturns}
\end{figure}

\subsection{弹性与等价性分析}

\subsubsection{弹性}

在基准点 $(N_0,D_0,Q_0)=(1.0\text{B},300\text{B},0.6)$ 用有限差分计算
对数弹性 $\varepsilon_x=\partial\ln L/\partial\ln x$：
\begin{equation}
\varepsilon_N=-0.060,\qquad \varepsilon_D=-0.030,\qquad
\varepsilon_Q=-0.146.
\label{eq:elasticity}
\end{equation}
质量弹性显著大于参数量与数据量弹性（绝对值约为参数量的 2.4 倍、数据量的
4.9 倍），直观表明：在基准点附近，改善数据质量是比增加参数/数据更高效的
能力提升途径——这为问题三的质量投入决策提供了定量依据。

\begin{figure}[htbp]
\centering
\includegraphics[width=0.8\textwidth]{figures/p2_elasticity.png}
\caption{基准点处三个维度的损失弹性对比（质量弹性最高）。}
\label{fig:p2_elasticity}
\end{figure}

\textbf{质量--规模替代的等值面}（图 \ref{fig:p2_subst}）：固定 $D=300$B，
画出 $L(N,Q)$ 等高线及``等损失等价轨迹''（保持 $L$ 不变的
$(N,Q)$ 组合）。等高线的局部切线斜率即替代率：基准点处
$dN/dQ=-2.43$B/单位$Q$（每提升 0.1 质量可减少 0.243B 参数需求），与
弹性比值 $|\varepsilon_Q/\varepsilon_N|=0.146/0.060\approx2.4$ 一致；
而沿轨迹向下，$N$ 越小所需补偿越少（$N$ 的 $-a$ 与 $-h$ 幂的凸性），
严格非线性等价为每 0.1 节省 0.216B（见 Bootstrap 节）。

\begin{figure}[htbp]
\centering
\includegraphics[width=0.8\textwidth]{figures/v36_p2_subst_contour.png}
\caption{质量--规模替代等值面：等损失轨迹显示质量对参数的替代关系。}
\label{fig:p2_subst}
\end{figure}

\textbf{替代率随规模的演化}（图 \ref{fig:p2_substscale}）：用隐函数定理
$dN/dQ=-L_Q/L_N$ 评估等损失面上替代率沿规模轴的分布：每 $+0.1Q$
等价可减少的参数从 $N=0.3$B 处的 0.063B 上升至 $N=1$B 处的 0.243B
（与等值面基准点一致）、$N=3$B 处约 0.6B——\textbf{替代率随规模近乎
线性上升}（比值 $eq/N$ 约 0.21--0.25，缓慢增长）。含义：质量购买力的
\textbf{绝对等价参数}随模型规模增大而增大（大模型下提升 0.1 质量节省
更多参数），但\textbf{相对份额}（等价参数/当前规模）约 0.24 保持稳定
——质量投入与规模投入的替代比率在大中小规模上同量级。

\begin{figure}[htbp]
\centering
\includegraphics[width=0.8\textwidth]{figures/v51_p2_subst_scale.png}
\caption{每 +0.1Q 等价减少的参数随规模的演化（近线性上升）。}
\label{fig:p2_substscale}
\end{figure}

\textbf{弹性排序对参考点的稳健性}（图 \ref{fig:p2_epsgrid}）：在
$(N_0,D_0,Q_0)$ 网格（$N_0\in[0.7,1.3]$B、$D_0\in[200,400]$B、
$Q_0\in[0.5,0.7]$，共 175 点）上以相同有限差分口径重算弹性。结果：
$|\varepsilon_Q|>|\varepsilon_N|$ 与 $|\varepsilon_Q|>|\varepsilon_D|$
在\textbf{全部 175 个参考点成立}（占比 1.000）；比值
$|\varepsilon_Q/\varepsilon_N|\in[2.23,2.62]$（中位 2.42）、
$|\varepsilon_Q/\varepsilon_D|\in[4.14,5.63]$（中位 4.89）——``质量弹性
最高、约为参数量 2.4 倍、数据量 4.9 倍''的结论\textbf{不是单点结论}，
在参考点全域保持，为问题三质量投入决策提供了跨参考点稳健的定量依据。

\begin{figure}[htbp]
\centering
\includegraphics[width=0.85\textwidth]{figures/v41_p2_eps_ref_grid.png}
\caption{175 个参考点上弹性分布与质量弹性相对优势比值。}
\label{fig:p2_epsgrid}
\end{figure}

\subsubsection{等价性}

定义``质量等价参数增量''：保持 $L$ 不变，$Q$ 提升 $\Delta Q$ 所需补偿的
参数增量 $\Delta N$，由全微分 $\partial L/\partial Q\cdot\Delta Q+
\partial L/\partial N\cdot\Delta N=0$ 得：
\begin{equation}
\Delta N=\Bigl|\frac{\partial L/\partial Q}{\partial L/\partial N}\Bigr|
\,\Delta Q.
\label{eq:equiv}
\end{equation}
在基准点处质量每提升 0.1 等价于增加 $\Delta N=0.289$B 参数（约 29\% 相对
增量），进一步说明质量杠杆的高效性。

\subsection{小结}

问题二完成了从经典标度律到广义标度律的推广：经典参数与文献一致
（$\alpha=0.34,\beta=0.28$），跨家族偏移修正验证 $R^2=0.83$；对四种候选
质量扩展形式统一拟合，选定\textbf{质量--参数量交互形式}
$L=E+A N^{-a}+B D^{-b}+C(1-Q)^{g}N^{-h}$ 在 B6+B7 上 $R^2=0.979$，
$Q=1$ 可退化，其物理含义为``模型越大对质量缺口越不敏感''；弹性分析表明
质量弹性最高（$-0.146$），质量 0.1 等价于 0.289B 参数；
并诊断出 B8 方向相反的数据口径问题，仅作结构对照。该广义标度律将作为
问题三优化的目标函数。
