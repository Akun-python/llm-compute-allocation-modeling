\section{模型评价与推广}

\subsection{模型的优点}

\begin{enumerate}
\item \textbf{数据驱动、可复算}：四问全部数值结果由真实附件数据计算，代码
随附录提供，任何结论均可复现；关键参数（$\alpha,\beta$）与文献报告值
（Chinchilla 的 0.34/0.28）一致，验证了数据与方法的可靠性；
\item \textbf{广义化框架}：把经典标度律推广到含质量维度，且 $Q=1$ 自动
退化，理论一致性良好；同时保证``质量''通过可解释的配比加权进入模型，
贯通问题一--三；
\item \textbf{严谨的数据口径核验}：诊断出附件 B8 的 $Q$ 方向与 B6/B7
相反、C3 历史前沿含孤立高值等数据问题，并给出规避与对照处理，避免了
半合成数据对结论的污染；
\item \textbf{结构性转移的显式数学刻画}：给出质量激活/成本主导两类转移
指标与 KKT 识别方法，使``质变''判断可计算、可复现；
\item \textbf{预测带不确定性}：前沿预测采用自助置信区间与双情景对比，
不给出伪精度的单点预言。
\end{enumerate}

\subsection{能力杠杆总览}

把四问的主要``投入--性能''弹性汇总为统一口径（图 \ref{fig:leverage}）：
损失/能力对各投入的对数弹性排序为——\textbf{上下文长度 $0.197$}、
\textbf{预算 $0.161$}、\textbf{质量 $0.146$}、时间趋势 $0.089$、
参数 $0.060$、数据 $0.030$。三条政策含义：
（i）\textbf{单投入杠杆中质量与上下文最高}：质量提升 0.1 单位损失下降约
$1.5\%$、上下文长度弹性约为参数量的一半，而预算翻倍的相对损失收益约
$10.5\%$（$\ln(L^*-E)$ 斜率 0.161）——质量/上下文投入的单位成本远低于
翻倍算力；（ii）\textbf{数据是最低杠杆}：$D$ 弹性仅 0.030，
叠加跨尺度收缩（$\kappa=0.314$）表明大模型下继续堆数据收益递减——与
``前沿模型已处数据过量区''（v24）一致；（iii）时间趋势 $b_T=0.089$ 反映
算法/基础设施的外生演进，不可通过内部预算直接购买，只能通过``预判--提前
储备''应对。该总览为第 7 节预算分配结论提供了跨问题的一致性坐标。

\begin{figure}[htbp]
\centering
\includegraphics[width=0.86\textwidth]{figures/v29_leverage_overview.png}
\caption{能力杠杆总览：各投入的对数弹性排序（质量/上下文 > 预算 > 参数/时间 > 数据）。}
\label{fig:leverage}
\end{figure}

\subsection{资源配置决策对照表}

把四问的核心结论汇总为可直接执行的决策对照（表 \ref{tab:decision}），
每行给出问题、核心决策、关键量化依据与稳健性来源。

\begin{table}[htbp]
\centering
\caption{四问资源配置决策对照表}
\label{tab:decision}
\small
\begin{tabular}{p{1.7cm}|p{3.4cm}|p{4.6cm}|p{3.2cm}}
\toprule
问题 & 核心决策 & 关键量化依据 & 稳健性来源 \\
\midrule
问题一 & 数据以 book/arxiv 为质量锚，剔除 commoncrawl 类低质域 & 域序 book 居首、21/21 对域间差异显著（$p<2.4\times10^{-3}$） & 八方法中加权/贴近度/折衷族序一致（两两 Spearman$\ge0.93$；RSR 序位略异）、5\% 抽样仍 100\% 保持 \\
问题二 & 质量提升是最高杠杆（$\varepsilon_Q=-0.146$） & 质量弹性约为参数 2.4 倍、数据 4.9 倍；质量 0.1 等价约 0.22--0.29B 参数 & 175 参考点全部保持、Bootstrap 区间不交叠 \\
问题三 & 预算充足时质量通道最优解 $Q^{*}\to1$，$N^{*}\sim C^{0.46}$ & 联合优化 vs 纯规模基线收益 6.7\%--0.8\%；log 成本形式最早饱和 & 收敛初值（13--17 个）100\% 落到同一最优 \\
问题四 & 前沿以规模扩张为主（约 84\%），推理能力增速最快 & $b_N=0.36$、$b_T=0.09$/年、MATH 年化 +0.89；2024--2025 增速反转 & 滚动窗口参数、自助区间、$g_N$ 敏感性双情景 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{模型的缺点与改进方向}

\begin{enumerate}
\item \textbf{半合成数据的局限}：B 族 $N$--$D$--$Q$ 数据为半合成实验，
B8 更与 B6/B7 口径不一致；改进方向：使用真实多质量档预训练实验校准 $Q$
项参数，并做交叉验证；
\item \textbf{广义标度律的可加性假设}：质量项与规模项的可加分解为近似，
当数据量极小或质量极低时交互效应可能显著；改进方向：引入 $N$--$Q$
交互项并比较拟合；
\item \textbf{质量成本函数参数的外生性}：$g(Q)$ 三种形式的 $\gamma,\lambda$
由题意给定且跨度大，实际成本取决于标注、过滤与合成支出；改进方向：用
真实 token 质量提升工程的成本数据标定；
\item \textbf{前沿预测的线性外推}：$\ln S\sim\ln N+t$ 的线性假设在 24 个月
后可能因算法饱和而失效；改进方向：引入饱和项（如 logit 上界）或滚动
再估计窗口；
\item \textbf{排行榜指标的选择偏差}：C1 的平均分受模型提交意愿、评测版本
影响；改进方向：用 C8 逐任务数据构建去偏的因子得分。
\end{enumerate}

\subsection{模型推广}

本文方法可推广到：多模态模型的数据质量评分与配比优化、RAG/微调阶段的质量
--成本权衡、企业算力采购的预算分配决策，以及``数据工程投入 vs 算力投入''
的一般资源规划问题。跨尺度系数收缩分析亦可用于其他``定量投入--性能''系统
（如推荐系统特征工程、模拟器 fidelity 选择），识别投入边际收益随规模的
衰减规律。